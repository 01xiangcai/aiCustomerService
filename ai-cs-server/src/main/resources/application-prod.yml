spring:
  # MySQL 生产环境 (匹配 docker-compose)
  datasource:
    url: jdbc:mysql://localhost:3307/ai_customer_service?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: yaoyuelong123.,

  # Redis 生产环境
  data:
    redis:
      host: localhost
      port: 6379
      password: yaoyuelong123.,
      database: 0

# MyBatis Plus 生产环境配置（关闭详细 SQL 日志）
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.nologging.NoLoggingImpl

# PGVector 生产环境 (匹配 docker-compose)
pgvector:
  url: jdbc:postgresql://localhost:5432/ai_customer_service
  username: root
  password: yaoyuelong123.,
